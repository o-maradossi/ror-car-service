%h1 All Cars
%hr
- if @cars.any?
  %ul
    - @cars.each do |car|
      %li
        = link_to car.make, car_path(car)
        = car.model
        = car.year
        %br
        = car.mileage 
        miles
- else
  %h3 Sorry, we don't have any cars right now